# ===== Secrets =====
.env
*.env
.env.*
*.pem

# ===== Python =====
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-airflow/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ===== Local data / logs =====
# NOTE: data lives in S3 (data lake) + Snowflake. These dirs are just a
# safety net for any future local scratch data. The `warehouse/` PYTHON
# PACKAGE (snowflake loader) must NOT be ignored, hence the -data suffix.
logs/
*.csv
*.parquet
raw/
# NOTE: anchored to repo root only (/staging/) so it does NOT swallow
# SPA_ANALYTICS_DBT/models/staging/sources.yml
/staging/
warehouse-data/

# ===== dbt =====
SPA_ANALYTICS_DBT/logs/
SPA_ANALYTICS_DBT/target/
SPA_ANALYTICS_DBT/dbt_packages/
SPA_ANALYTICS_DBT/.user.yml
profiles.yml

# ===== Airflow (local machine state) =====
airflow/airflow.db
airflow/airflow.cfg
airflow/webserver_config.py

# ===== OpenCode scratch / personal =====
opencode_orch/

# ===== Scratch scripts =====
scripts/temp_query.py

# ===== IDE / OS =====
.idea/
.vscode/
.DS_Store
Thumbs.db

