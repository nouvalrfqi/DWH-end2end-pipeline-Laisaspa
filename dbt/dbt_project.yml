name: 'dbt'
version: '1.0.0'
profile: 'dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt:
    warehouse:
      +materialized: table
    mart:
      +materialized: table
      +schema: MART